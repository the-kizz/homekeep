/**
 * Phase 2 Plan 3 placeholder for the /h landing. 02-04 replaces this with
 * the real homes list + HOME-03 last-viewed redirect. Kept minimal so the
 * auth E2E can land here after signup/login.
 */
export default function HomesLandingPage() {
  return (
    <div className="space-y-3 p-6">
      <h1 className="text-2xl font-semibold">Your homes</h1>
      <p className="text-muted-foreground">
        Empty state — homes CRUD lands in 02-04.
      </p>
    </div>
  );
}
