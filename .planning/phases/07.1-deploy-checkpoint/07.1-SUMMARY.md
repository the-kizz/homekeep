---
phase: 07.1-deploy-checkpoint
status: complete
completed: 2026-04-21T09:48:00Z
live_url: http://46.62.151.57:3000/
admin_url: http://46.62.151.57:3000/_/
version: v1.0.0-rc1
final: true
---

# Phase 7.1 — v1 Release Candidate Deploy

HomeKeep v1.0.0-rc1 is live at **http://46.62.151.57:3000/**. All 7 phases shipped.

## Live endpoints
- / (landing + auth gate) → 200
- /login, /signup → 200
- /api/health → {status:ok,nextjs:ok,pocketbase:ok}
- /manifest.webmanifest → 200 (PWA installable over HTTPS once a Caddy/Tailscale variant is used)

## Feature state
- Phase 1: Docker + Next.js 16 + PocketBase 0.37.1 + Caddy + s6-overlay + multi-arch CI
- Phase 2: Signup/login, homes, areas (with drag-reorder), tasks, computed next-due
- Phase 3: Three-band dashboard + one-tap complete + early-completion guard + coverage ring
- Phase 4: Invite links, members, cascading task assignment (owner + members)
- Phase 5: By Area / Person / History views + onboarding wizard + seed library
- Phase 6: ntfy notifications + node-cron scheduler + streak badge + celebration + most-neglected card
- Phase 7: PWA manifest + service worker + HTTP-context banner + Caddy/Tailscale compose overlays + docs

## Release path (operator action — not auto-run)
```bash
# Once GitHub remote + GHCR PAT are configured:
git tag v1.0.0-rc1
git push origin v1.0.0-rc1
# → release.yml builds multi-arch amd64+arm64 → pushes ghcr.io/<owner>/homekeep:v1.0.0-rc1 + latest
```

## Verification totals
- 311 unit tests green (44 suites)
- 23 E2E Playwright tests green
- 9 PocketBase migrations + 4 hooks baked into the image
- Image size: 105MB (limit: 300MB per SPEC §15)
- Data persistence: docker_homekeep_data named volume

## Known post-v1
- Serwist-generated public/sw.js has lint noise (pre-existing; deferred)
- arm64 cross-build still running under QEMU locally (CI will validate on every PR)
- Real HTTPS deploy requires user to run compose with docker-compose.caddy.yml overlay + point a domain at the VPS
