---
phase: 01-scaffold-infrastructure
plan: 07
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - README.md
autonomous: true
requirements:
  - INFR-12
user_setup: []

must_haves:
  truths:
    - "README.md contains a Quickstart section with BOTH `docker run` (standalone per D-04) AND `docker compose up` commands, each with the full flag set including `-p 80:3000 -v ./data:/app/data --env-file .env ghcr.io/OWNER/homekeep:latest`"
    - "README.md documents the first-boot PocketBase superuser flow: `docker compose logs homekeep | grep -i installer` per RESEARCH.md Open Question #4 recommendation"
    - "README.md documents the dev workflow: `npm install && npm run dev` starts Next.js on :3001 and PocketBase on :8090 via scripts/dev-pb.js"
    - "README.md documents `.env.example` variables: SITE_URL, NTFY_URL, TZ, PUID, PGID with purpose of each"
    - "README.md includes a SQLite + local-filesystem warning per Pitfall #1 (data volume must not be on NFS/SMB)"
    - "README.md includes an MIT license badge link to LICENSE and the `ghcr.io/OWNER/homekeep` image path"
    - "README.md references SPEC.md and .planning/ROADMAP.md for further reading"
    - "README.md documents PUID=1000 default and instructs non-1000 UID hosts to chown the data dir per Pitfall #9"
    - "README.md documents the three commands for building the image locally: `npm run docker:build`, `npm run docker:run`, `docker compose -f docker/docker-compose.yml up -d`"
  artifacts:
    - path: "README.md"
      provides: "Public-facing documentation covering quickstart, config, dev workflow, first-boot PB setup, and license; finalized for INFR-12 public-repo polish"
      contains: "ghcr.io/"
      min_lines: 80
  key_links:
    - from: "README.md Quickstart"
      to: "docker/docker-compose.yml"
      via: "docker compose -f docker/docker-compose.yml up -d"
      pattern: "docker compose .*docker/docker-compose\\.yml"
    - from: "README.md Quickstart"
      to: "ghcr.io/OWNER/homekeep"
      via: "docker run standalone command"
      pattern: "docker run .*ghcr\\.io/.*/homekeep"
    - from: "README.md"
      to: "LICENSE"
      via: "License link"
      pattern: "\\(LICENSE\\)|\\[LICENSE\\]|\\./LICENSE"
    - from: "README.md First-boot section"
      to: "PocketBase installer link"
      via: "docker logs grep pattern"
      pattern: "docker.*logs.*installer"
---

<objective>
Replace the stub README.md (created in 01-01) with the public-repo-polish README that documents: quickstart for both `docker run` and `docker compose up` (D-04), dev workflow (D-06), environment variable reference (INFR-10/11), first-boot PocketBase admin setup (RESEARCH.md Open Question #4), key operational caveats (Pitfall #1 filesystem, Pitfall #9 permissions), and license/repo metadata (INFR-12).

Purpose: INFR-12 requires a public MIT-licensed GitHub repo; a discoverable README is the primary artifact visitors see. Closes the Phase-1 documentation gap and is the last piece before the phase can be marked verified. Depends on all previous Phase-1 plans because it must document actual paths and commands that exist (`scripts/dev-pb.js`, `docker/docker-compose.yml`, `docker/Dockerfile`, package.json scripts, `.github/workflows/release.yml` tag scheme).

Output: One finalized `README.md` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffold-infrastructure/01-CONTEXT.md
@.planning/phases/01-scaffold-infrastructure/01-RESEARCH.md
@.planning/research/PITFALLS.md
@SPEC.md
@package.json
@.env.example
@docker/docker-compose.yml
@docker/Dockerfile
@scripts/dev-pb.js
@LICENSE

<interfaces>
<!-- README consumes these artifacts; document them faithfully -->

From 01-01:
- package.json scripts: dev, dev:next, dev:pb, build, start, lint, typecheck, test, test:watch, test:e2e, docker:build, docker:run
- .env.example variables: SITE_URL, NTFY_URL, TZ, PUID, PGID
- LICENSE: MIT

From 01-02:
- docker/Dockerfile multi-arch via TARGETARCH
- scripts/check-image-size.sh (300MB budget)

From 01-03:
- s6-overlay supervision (document as "process management inside the container")

From 01-04:
- Caddy routes /api/*, /_/*, catch-all (document that PB admin at /_/ is accessible and intended)

From 01-05:
- docker/docker-compose.yml LAN-only variant
- scripts/dev-pb.js

From 01-06:
- GHCR image path: ghcr.io/OWNER/homekeep (document placeholder OWNER; users customize via GHCR_OWNER env var)
- Release tags: {version}, {major}.{minor}, latest

Quickstart commands that MUST appear in the README:
```bash
# docker run standalone (D-04)
docker run --rm \
  -p 80:3000 \
  -v "$(pwd)/data:/app/data" \
  --env-file .env \
  ghcr.io/OWNER/homekeep:latest

# docker compose (D-04)
cp .env.example .env
docker compose -f docker/docker-compose.yml up -d

# First-boot PB admin (Open Question #4)
docker compose -f docker/docker-compose.yml logs homekeep | grep -i installer

# Dev workflow (D-06)
npm install
npm run dev
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write public-polish README.md covering quickstart, dev, config, first-boot, caveats, and license</name>
  <files>README.md</files>
  <read_first>
    README.md (the stub from 01-01),
    .planning/phases/01-scaffold-infrastructure/01-CONTEXT.md (D-04, D-05, D-06, D-09, D-11),
    .planning/phases/01-scaffold-infrastructure/01-RESEARCH.md (Open Questions #4 and #5, §Pitfall 1, §Pitfall 9),
    .planning/research/PITFALLS.md (§Pitfall 1, §Pitfall 9, §Pitfall 15),
    SPEC.md (§13-16),
    package.json,
    .env.example,
    docker/docker-compose.yml,
    LICENSE
  </read_first>
  <action>
Overwrite the README.md stub from 01-01 with the full public README below. Keep wording calm and utilitarian -- matches HomeKeep's product personality (warm, domestic; not SaaS-cockpit per SPEC §19). Use concrete commands, not prose. No emoji. Include badges block at the top.

Write this exact content:

```markdown
# HomeKeep

[![CI](https://github.com/OWNER/homekeep/actions/workflows/ci.yml/badge.svg)](https://github.com/OWNER/homekeep/actions/workflows/ci.yml)
[![Release](https://github.com/OWNER/homekeep/actions/workflows/release.yml/badge.svg)](https://github.com/OWNER/homekeep/actions/workflows/release.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](./LICENSE)

Household maintenance that is visible, evenly distributed, and nothing falls through the cracks -- without anxiety or guilt.

Self-hosted. One Docker container. One folder to back up.

## Status

**Phase 1 of 7 -- Scaffold & Infrastructure.** The container builds, boots, and answers `/api/health`. No application features yet. See [ROADMAP](./.planning/ROADMAP.md) for the full plan.

## Quickstart

### With `docker run` (standalone)

```bash
# On any host with Docker:
cp .env.example .env
docker run -d \
  --name homekeep \
  -p 80:3000 \
  -v "$(pwd)/data:/app/data" \
  --env-file .env \
  ghcr.io/OWNER/homekeep:latest
```

Replace `OWNER` with the GitHub user or org that published the image. Map `80:3000` to any host port you like (the app listens on 3000 inside the container).

### With Docker Compose

```bash
cp .env.example .env
docker compose -f docker/docker-compose.yml up -d
```

Compose reads `HOST_PORT`, `GHCR_OWNER`, `TAG`, and `TZ` from `.env` (see `.env.example`). Defaults: `HOST_PORT=3000`, `TAG=latest`.

### First boot: create the PocketBase admin

After the container starts, PocketBase prints a one-time installer link to its logs. Open it to set up the superuser:

```bash
# Tail the logs for the installer URL
docker compose -f docker/docker-compose.yml logs homekeep | grep -i installer
# or for docker run:
docker logs homekeep | grep -i installer
```

Open the URL in a browser (replace `127.0.0.1:8090` with your host's address and port, e.g. `http://192.168.1.10/_/`). Pick an email and password. You are now the PocketBase admin; the admin UI lives at `/_/`.

## Configuration

All runtime configuration is env-driven. Copy `.env.example` to `.env` and edit:

| Variable | Purpose | Default |
|----------|---------|---------|
| `SITE_URL` | Public URL users reach the app at; used for PWA manifest and ntfy notification links | `http://localhost:3000` |
| `NTFY_URL` | ntfy server base URL (push notifications, Phase 6+) | `https://ntfy.sh` |
| `TZ` | IANA timezone for scheduler and date display | `Etc/UTC` |
| `PUID` | Host UID that owns the `./data` volume | `1000` |
| `PGID` | Host GID that owns the `./data` volume | `1000` |

No secrets are hardcoded anywhere in the image. `.env.example` is committed; real `.env` is gitignored.

### If your host UID is not 1000

The default `node` user inside the container is UID 1000. If your host user has a different UID, use **either** of the following one-time fixes (future versions will honor `PUID`/`PGID` at runtime -- Phase 7):

**Option A -- chown from inside the container** (explicitly overrides the s6 `/init` entrypoint with `sh` so the chown can actually run -- `/init` alone would just start the services and never execute the trailing command):

```bash
docker run --rm --entrypoint sh -u 0 \
  -v ./data:/app/data \
  ghcr.io/OWNER/homekeep:latest \
  -c "chown -R 1000:1000 /app/data"
```

**Option B -- chown from the host** (no container involved):

```bash
mkdir -p data && sudo chown -R 1000:1000 data
```

Either option leaves `./data/` owned by UID 1000, which matches the `node` user inside the container.

## Backup

The entire application state lives in `./data/`. Back it up by stopping the container and copying the folder:

```bash
docker compose -f docker/docker-compose.yml down
cp -a data/ backups/data-$(date +%Y%m%d)/
docker compose -f docker/docker-compose.yml up -d
```

**Important:** `./data/` must live on a local filesystem. NFS, SMB, and most NAS-mounted paths will silently corrupt the SQLite WAL. If your host is a Synology or Unraid, use a local docker volume rather than a bind mount to a shared path.

## Development

Day-to-day development runs natively (no Docker). Docker is only for building the production image.

```bash
# Requirements: Node 22+, unzip (for PocketBase download)
npm install
npm run dev
```

`npm run dev` uses `concurrently` to run two processes:
- **Next.js** on `http://localhost:3001` (dev server)
- **PocketBase** on `http://127.0.0.1:8090` (via `scripts/dev-pb.js`, which downloads the binary into `./.pb/` on first run)

Other scripts:

| Command | Purpose |
|---------|---------|
| `npm run dev:next` | Just Next.js |
| `npm run dev:pb` | Just PocketBase |
| `npm run lint` | ESLint via `next lint` |
| `npm run typecheck` | `tsc --noEmit` |
| `npm run test` | Vitest unit + integration tests |
| `npm run test:watch` | Vitest watch mode |
| `npm run test:e2e` | Playwright E2E tests |
| `npm run build` | Next.js production build |
| `npm run docker:build` | Build the Docker image locally (amd64) |
| `npm run docker:run` | Run the locally built image |

## Releases

Tagged commits on `main` trigger a multi-arch (linux/amd64 + linux/arm64) image build and push to GHCR:

```bash
git tag v0.1.0
git push origin v0.1.0
```

Images are published as:
- `ghcr.io/OWNER/homekeep:v0.1.0` (exact)
- `ghcr.io/OWNER/homekeep:0.1` (major.minor)
- `ghcr.io/OWNER/homekeep:latest` (default branch)

## Architecture

One container, two (plus one) processes managed by [s6-overlay v3](https://github.com/just-containers/s6-overlay):

- **Caddy** (port 3000, exposed) -- internal reverse proxy. Routes `/api/health` to Next.js, `/api/*` and `/_/*` to PocketBase with SSE-safe flushing, everything else to Next.js.
- **Next.js** (port 3001, loopback) -- UI and the combined `/api/health` endpoint.
- **PocketBase** (port 8090, loopback) -- SQLite database, auth, REST API, realtime, admin UI.

Only port 3000 is exposed. The browser talks to both Next.js and PocketBase via the same origin.

See [`SPEC.md`](./SPEC.md) for the full architectural spec.

## Verifying your deployment

After `up -d`, check the health endpoint:

```bash
curl -s http://localhost:3000/api/health
# { "status": "ok", "nextjs": "ok", "pocketbase": "ok", "pbCode": 200 }
```

If `pocketbase` is anything other than `"ok"`, check container logs.

## License

MIT -- see [LICENSE](./LICENSE).

## Contributing

This is an early-stage project. Check the [ROADMAP](./.planning/ROADMAP.md) and open an issue before sending PRs.
```

Invariants (enforced by acceptance_criteria):
- Starts with `# HomeKeep` h1
- Badges block with CI, Release, MIT License
- Quickstart with both `docker run` and `docker compose up` commands (D-04)
- First-boot section documenting the `grep -i installer` pattern for PB admin setup
- Configuration table documenting all five .env.example variables
- Pitfall #1 warning: `./data/` must be on local filesystem (no NFS/SMB)
- Pitfall #9 guidance: PUID 1000 default, chown alternative for other UIDs
- Dev workflow: `npm install && npm run dev` (D-06), scripts table
- Releases: tagged semver -> GHCR (matches 01-06 metadata-action output shape)
- Architecture summary of the three internal processes
- `/api/health` verification curl snippet
- Link to [LICENSE](./LICENSE) (MIT, INFR-12)
- Uses placeholder `OWNER` everywhere the repo path appears (user replaces in their fork/deployment)
- NO emoji (SPEC §19 aesthetic guard)
- Plain utilitarian tone matching product personality

DO NOT include:
- Promotional marketing copy or hype adjectives
- Features that don't exist yet (no auth screenshots, no "get started with your first home" -- those are Phase 2+)
- Dark-mode-first SaaS-cockpit language
- Phase 7 stuff (Tailscale, Caddy external, PWA install) -- they don't work yet
  </action>
  <verify>
    <automated>test -f README.md && grep -q '^# HomeKeep' README.md && grep -q 'docker run' README.md && grep -q 'docker compose' README.md && grep -q 'ghcr.io/' README.md && grep -q 'grep -i installer' README.md && grep -q 'SITE_URL' README.md && grep -q 'NTFY_URL' README.md && grep -q 'TZ' README.md && grep -q 'PUID' README.md && grep -q 'npm run dev' README.md && grep -q 'LICENSE' README.md && grep -q 'MIT' README.md && grep -q 'local filesystem' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md`
    - `head -1 README.md | grep -q '^# HomeKeep'`
    - `grep -q '!\[CI\](' README.md` OR `grep -q 'badge\.svg' README.md` (badges present)
    - `grep -q 'License: MIT' README.md` OR `grep -q 'MIT License' README.md`
    - Quickstart section: `grep -qE '^## Quickstart' README.md`
    - Docker run command: `grep -q 'docker run' README.md && grep -q 'ghcr.io/' README.md && grep -q '\-\-env-file \.env' README.md`
    - Docker compose command: `grep -q 'docker compose -f docker/docker-compose.yml up' README.md`
    - First-boot section: `grep -qi 'first boot' README.md && grep -q 'grep -i installer' README.md`
    - Configuration table documenting all 5 env vars: `grep -q 'SITE_URL' README.md && grep -q 'NTFY_URL' README.md && grep -qE '\bTZ\b' README.md && grep -q 'PUID' README.md && grep -q 'PGID' README.md`
    - Pitfall #1 warning: `grep -qE '(local filesystem|NFS|SMB)' README.md`
    - Pitfall #9 guidance: `grep -q 'UID 1000' README.md` OR `grep -q 'UID is not 1000' README.md`
    - Dev workflow: `grep -qE '^## Development' README.md && grep -q 'npm install' README.md && grep -q 'npm run dev' README.md`
    - Scripts table: `grep -q 'npm run test' README.md && grep -q 'npm run build' README.md && grep -q 'npm run lint' README.md && grep -q 'npm run typecheck' README.md`
    - Releases section: `grep -qE '^## Releases' README.md && grep -q 'git tag v' README.md`
    - Architecture section: `grep -q 's6-overlay' README.md && grep -q 'Caddy' README.md && grep -q 'PocketBase' README.md`
    - Health verification: `grep -q 'curl.*api/health' README.md`
    - License link: `grep -qE '\(\.\/LICENSE\)' README.md`
    - Link to ROADMAP: `grep -q '\.planning/ROADMAP\.md' README.md`
    - Link to SPEC: `grep -q 'SPEC\.md' README.md`
    - No emoji: `! grep -Pq '[\x{1F300}-\x{1FAFF}]' README.md` (Perl regex for emoji block)
    - Minimum length: `[ $(wc -l < README.md) -ge 80 ]`
  </acceptance_criteria>
  <done>
    README.md is the public-facing Phase-1 documentation: badges, both D-04 quickstart flows, env config table, first-boot PB admin guidance, filesystem + UID caveats, dev scripts reference, release tagging scheme, architecture summary, health-check verification, and MIT license. Covers INFR-12 "MIT license, public GitHub repo" requirement and prepares the repo for public discovery.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GitHub repo -> README readers | Any misstatement could mislead operators |
| Documentation -> reader's deployment | Bad guidance could cause data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Information Disclosure | Example command shows a secret | mitigate | README uses only non-secret placeholders (OWNER for image path, generic `.env`); no tokens, no real credentials |
| T-01-07-02 | Tampering | README instructs readers to run untrusted commands | mitigate | Every command uses official images (ghcr.io/OWNER/homekeep from the user's own repo) and official tools (`docker`, `npm`) -- no curl-pipe-sh patterns |
| T-01-07-03 | Data Loss | User bind-mounts ./data on NFS and SQLite corrupts | mitigate | Explicit "local filesystem only" warning with named bad filesystems (NFS, SMB) per Pitfall #1 |
| T-01-07-04 | Permission Denied | User's host UID != 1000 and container cannot write | mitigate | Dedicated subsection "If your host UID is not 1000" with two alternative commands per Pitfall #9 |
| T-01-07-05 | Confusion / Support Load | User expects Phase 2+ features (auth, tasks) and reports missing-feature bugs | mitigate | "Status" section prominently explains Phase 1 delivers scaffold only; links to ROADMAP for the full plan |
</threat_model>

<verification>
Static verification:
```bash
# Structure
head -1 README.md | grep -q '^# HomeKeep'
# Required sections
for section in Quickstart Configuration Backup Development Releases Architecture License; do
  grep -qE "^## $section" README.md || echo "MISSING: ## $section"
done
# No emoji
! grep -Pq '[\x{1F300}-\x{1FAFF}]' README.md
# Key commands
grep -q 'docker compose -f docker/docker-compose.yml up -d' README.md
grep -q 'grep -i installer' README.md
grep -qE 'curl.*api/health' README.md
# Length sanity
[ $(wc -l < README.md) -ge 80 ]
```
</verification>

<success_criteria>
- README.md rewrites the 01-01 stub into the public-polish version covering: quickstart (both D-04 flows), env config (all five .env.example vars), backup procedure with Pitfall #1 warning, UID guidance per Pitfall #9, dev workflow (D-06), release tag scheme (matches 01-06 metadata-action), architecture summary, health verification, MIT license
- No emoji; utilitarian tone matching SPEC §19 aesthetic
- Links to LICENSE, SPEC.md, ROADMAP.md
- Badges present (CI, Release, MIT License)
- Placeholder `OWNER` for repo path (user replaces)
- Covers requirement: INFR-12 (public repo polish, MIT license reference)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold-infrastructure/01-07-SUMMARY.md` documenting: the final README.md content, any additional operational caveats surfaced during documentation that should feed back into RESEARCH.md's assumptions log, and a note confirming the phase's `ROADMAP.md` success criteria are all documented and discoverable.
</output>
