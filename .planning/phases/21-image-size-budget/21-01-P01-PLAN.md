---
phase: 21-image-size-budget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-image-size.sh
  - SPEC.md
  - .planning/PROJECT.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - INFRA-BUMP-01
tags:
  - infra
  - docker
  - ci
  - policy

must_haves:
  truths:
    - "scripts/check-image-size.sh enforces LIMIT=320 (not 300)"
    - "SPEC.md §15 states final image target is under 320MB with v1.1 rationale"
    - "PROJECT.md Constraints bullet states Docker image under 320MB with cross-ref to SPEC.md §15"
    - "CI workflow step label reads '<320MB per INFR-03' (cosmetic consistency)"
    - "Next CI run on push passes the image-size gate (309MB observed < 320MB limit)"
  artifacts:
    - path: "scripts/check-image-size.sh"
      provides: "CI image-size enforcement"
      contains: "LIMIT=320"
    - path: "SPEC.md"
      provides: "Canonical §15 Docker budget statement + v1.1 rationale"
      contains: "under 320MB"
    - path: ".planning/PROJECT.md"
      provides: "Forward project-level constraint record"
      contains: "under 320MB"
    - path: ".github/workflows/ci.yml"
      provides: "CI step label alignment with new budget"
      contains: "<320MB per INFR-03"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-image-size.sh"
      via: "sh scripts/check-image-size.sh homekeep:ci"
      pattern: "scripts/check-image-size\\.sh"
    - from: ".planning/PROJECT.md"
      to: "SPEC.md §15"
      via: "inline cross-reference in Constraints bullet"
      pattern: "SPEC\\.md §15"
---

<objective>
Raise the INFR-03 Docker image budget ceiling from 300MB to 320MB across the four forward source-of-truth files and update the CI step label cosmetically. This is a pure policy/documentation change — no application code, no Dockerfile, no workflow logic changes. The next CI push goes green because the current 309MB image falls under the new 320MB limit.

Purpose: Unblock CI + the `:latest` / `:1.1` GHCR tiered-tag advancement without attempting docker-layer optimization (deferred to v1.2 per D-03). Codify the v1.1 image growth (~9MB — 8 shadcn components, @radix-ui/react-collapsible, 4 new pb_migrations, expanded pb_hooks) as a deliberate, documented policy adjustment.

Output:
- `scripts/check-image-size.sh` with `LIMIT=320`
- `SPEC.md` §15 bullet raised + v1.1 rationale sub-bullet appended
- `.planning/PROJECT.md` Constraints bullet raised with SPEC.md cross-ref
- `.github/workflows/ci.yml` L77 step label aligned (`<300MB` → `<320MB`)
- No immutable history artifacts touched (REQUIREMENTS.md, ROADMAP.md, research/, phase summaries)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-image-size-budget/21-CONTEXT.md
@.planning/phases/21-image-size-budget/21-RESEARCH.md
@.planning/phases/21-image-size-budget/21-VALIDATION.md

@scripts/check-image-size.sh
@SPEC.md
@.github/workflows/ci.yml

<interfaces>
<!-- Exact current-state anchors. Match these verbatim before editing so replacements are deterministic. -->

From scripts/check-image-size.sh (entire file, 13 lines — the `LIMIT=300` literal is on line 6):
```sh
#!/bin/sh
set -eu
IMAGE="${1:-homekeep:test}"
BYTES=$(docker inspect "$IMAGE" --format '{{.Size}}')
MB=$((BYTES / 1024 / 1024))
LIMIT=300
echo "Image $IMAGE = ${MB}MB (limit ${LIMIT}MB)"
if [ "$MB" -gt "$LIMIT" ]; then
  echo "FAIL: image exceeds ${LIMIT}MB"
  exit 1
fi
echo "OK"
```

From SPEC.md around line 469 (the "§15 Docker + distribution requirements" bulleted list):
```markdown
## 15. Docker + distribution requirements

- **Multi-arch image:** `linux/amd64` + `linux/arm64` (Raspberry Pi, Apple Silicon, ARM NAS)
- **Final image target:** under 300MB
- **Single data volume:** `./data` contains PocketBase DB + uploaded photos. Backup = copy folder.
```

From .planning/PROJECT.md around line 80 (inside `## Constraints`):
```markdown
## Constraints

- **Tech stack**: Next.js 15 (App Router, standalone), PocketBase, Tailwind + shadcn/ui — specified in SPEC.md
- **Container**: Single Docker image under 300MB, serves both processes
- **Data**: All state in one `./data` volume — backup = copy folder
```

From .github/workflows/ci.yml around line 77 (the image-size step — note line 78 is already correct, only the label changes):
```yaml
      - name: Image size check (<300MB per INFR-03)
        run: sh scripts/check-image-size.sh homekeep:ci
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump INFR-03 image budget 300MB → 320MB across four forward source-of-truth files</name>
  <files>scripts/check-image-size.sh, SPEC.md, .planning/PROJECT.md, .github/workflows/ci.yml</files>
  <read_first>
    Read these four files in full before editing (they are small):
    - scripts/check-image-size.sh (13 lines)
    - SPEC.md lines 460-480 (the §15 block; confirm line 469 anchor)
    - .planning/PROJECT.md lines 72-90 (the Constraints block; confirm line 80 anchor)
    - .github/workflows/ci.yml lines 70-85 (the image-size-check step; confirm line 77 anchor)
    Also re-read .planning/phases/21-image-size-budget/21-RESEARCH.md "Exact Edit Targets" table — the 4-row table at L41-46 is your authoritative edit list.
  </read_first>
  <action>
    Perform exactly four edits. Do NOT touch any other file. In particular, do NOT modify REQUIREMENTS.md, ROADMAP.md, research/*, or any phase/*-SUMMARY.md / *-VERIFICATION.md / *-CONTEXT.md — those are immutable history (per D-01 / D-02 / D-04 and RESEARCH.md "Immutable history — DO NOT EDIT" list).

    **Edit 1 — scripts/check-image-size.sh (line 6)**
    Use Edit with:
    - old_string: `LIMIT=300`
    - new_string: `LIMIT=320`
    This is the ONLY occurrence of `LIMIT=300` in the script (verified in research). The surrounding echo uses `${LIMIT}MB` interpolation, so no other edit is needed in this file.

    **Edit 2 — SPEC.md (line 469, §15 Docker + distribution requirements)**
    Use Edit with:
    - old_string: `- **Final image target:** under 300MB`
    - new_string (appends a sub-bullet explaining v1.1 growth per D-02):
      ```
      - **Final image target:** under 320MB (raised from 300MB in v1.1)
        - v1.1 growth (~9MB): 8 new shadcn/ui components (Collapsible, Dialog, DropdownMenu variations), `@radix-ui/react-collapsible`, 4 new `pb_migrations`, expanded `pb_hooks`. Further optimization deferred to v1.2 (separate docker-layer pass).
      ```
    The sub-bullet is indented with 2 spaces to nest under the parent bullet (matching the surrounding markdown list style).

    **Edit 3 — .planning/PROJECT.md (line 80, Constraints bullet)**
    Use Edit with:
    - old_string: `- **Container**: Single Docker image under 300MB, serves both processes`
    - new_string: `- **Container**: Single Docker image under 320MB (raised from 300MB in v1.1 — see SPEC.md §15), serves both processes`
    This keeps the bullet on one line (matching the surrounding Constraints list style) and points readers to SPEC.md §15 for the full rationale (so PROJECT.md stays terse).

    **Edit 4 — .github/workflows/ci.yml (line 77, step label only — the `run:` line 78 is unchanged)**
    Use Edit with:
    - old_string: `      - name: Image size check (<300MB per INFR-03)`
    - new_string: `      - name: Image size check (<320MB per INFR-03)`
    Preserve the exact leading 6-space indentation. Do NOT touch line 78 (`run: sh scripts/check-image-size.sh homekeep:ci`) — the script name and invocation are correct as-is.

    After all four edits, run the grep invariants from .planning/phases/21-image-size-budget/21-VALIDATION.md "Grep-Based Structural Invariants" section and confirm every count matches.

    **Why this plan skips `tdd="true"`:** this task only touches a shell constant, two markdown docs, and a YAML step label. No unit-testable production code is produced. Acceptance is grep-verifiable (static invariants) plus the CI run on the next push. See VALIDATION.md for the full acceptance matrix.

    **Reference:** Implements D-01 (new budget = 320MB), D-02 (rationale documented), D-03 (no docker optimization this phase), D-04 (no retroactive rebuild). All four edits correspond to the RESEARCH.md "Exact Edit Targets" table.
  </read_first>
  <verify>
    <automated>
      # Run from repo root. ALL seven checks must pass.
      test "$(grep -c '^LIMIT=320$' scripts/check-image-size.sh)" = "1" \
        && test "$(grep -c '^LIMIT=300$' scripts/check-image-size.sh)" = "0" \
        && test "$(grep -c 'under 320MB' SPEC.md)" -ge "1" \
        && test "$(grep -c 'v1.1 growth' SPEC.md)" -ge "1" \
        && test "$(grep -c 'under 320MB' .planning/PROJECT.md)" = "1" \
        && test "$(grep -c 'SPEC.md §15' .planning/PROJECT.md)" -ge "1" \
        && test "$(grep -c 'Image size check (&lt;320MB per INFR-03)' .github/workflows/ci.yml)" = "1" \
        && echo "ALL 7 INVARIANTS PASS"
    </automated>
  </verify>
  <done>
    1. `scripts/check-image-size.sh` line 6 reads `LIMIT=320`.
    2. `SPEC.md` §15 first bullet reads `under 320MB` with an indented v1.1 rationale sub-bullet referencing ~9MB growth (shadcn, @radix-ui/react-collapsible, pb_migrations, pb_hooks) and deferring further optimization to v1.2.
    3. `.planning/PROJECT.md` Constraints bullet reads `Single Docker image under 320MB (raised from 300MB in v1.1 — see SPEC.md §15), serves both processes`.
    4. `.github/workflows/ci.yml` step label at L77 reads `Image size check (<320MB per INFR-03)`; L78 `run:` line is untouched.
    5. `git diff --name-only` shows exactly these four paths — no others.
    6. No matches remain for `LIMIT=300` in `scripts/` and `under 300MB` in `SPEC.md` / `.planning/PROJECT.md` (immutable history files are allowed to retain 300MB references and MUST NOT be edited).
  </done>
</task>

</tasks>

<verification>
All acceptance verification lives in `.planning/phases/21-image-size-budget/21-VALIDATION.md` — the REQ→check map, grep invariants, and local vs. CI acceptance bands.

Local phase gate (before commit):
1. `sh scripts/check-image-size.sh` (dry inspection — this command requires a built image; skip if none present locally. Not a blocker for commit.)
2. Grep invariants (see `<verify>` block above) — all seven PASS.
3. `git diff --name-only` lists only the four files listed in `files_modified`.

Post-push gate (closes INFRA-BUMP-01):
- `.github/workflows/ci.yml` run triggered by the push: `lint-test-build` job green, `Image size check (<320MB per INFR-03)` step green (309MB observed passes 320MB limit).
- GHCR tiered-tag advancement on the next stable tag push (release.yml handles this automatically once CI is green — out of scope for this phase but unblocked by it).
</verification>

<success_criteria>
- [ ] INFRA-BUMP-01 addressed (forward policy raised 300MB → 320MB)
- [ ] All four files edited exactly as specified; no others touched
- [ ] Seven grep invariants all PASS (see `<verify>` above)
- [ ] Local `git diff --name-only` returns the four expected paths and nothing else
- [ ] Next CI run on push is green on the `Image size check` step
- [ ] Immutable history (REQUIREMENTS.md, ROADMAP.md, .planning/research/*, prior phase artifacts) is untouched — verify via `git diff --stat` before committing
</success_criteria>

<output>
After completion, create `.planning/phases/21-image-size-budget/21-01-P01-SUMMARY.md` following the standard summary template. Include:
- Exact diffs (4 files, small — paste them verbatim in the summary)
- Grep-invariant pass output
- Note that the post-push CI run is the forward gate (the commit creating this summary should be the one that turns CI green)
- Any deviations (none expected — this is a surgical edit)
</output>
